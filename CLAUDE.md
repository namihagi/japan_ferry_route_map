# 日本フェリー航路マップ

日本国内の定期旅客航路を地図に描く静的サイト（GitHub Pages で公開）。個人の趣味サイトで、情報の正確さはベストエフォート。

## 最初に読むもの

- **用語**：[`CONTEXT.md`](CONTEXT.md)。航路、区間、港、寄港地、照合済みなどの意味はここに従う。コード、データ、コミットメッセージの語彙もこれに揃える。
- **仕様**：[`docs/spec.md`](docs/spec.md)。初版で作るもの・作らないもの、データの作り方、マイルストーン。
- **判断の理由**：[`docs/adr/`](docs/adr/)。既存の設計を変えたくなったら、先に該当する ADR を読む。

## ガードレール

1. **リポジトリに入れるデータは、再配布できるライセンスのものだけにする。**
   - 国土数値情報（N09 定期旅客航路、C02 港湾、C23 海岸線など、非商用ライセンスのもの）と、そこから派生したデータは `.cache/` に置き、ローカルでの参照だけに使う（ADR 0001）。
   - `data/` は ODbL。OSM 由来のデータと、自分で作ったデータだけを置く。
2. **地図に公開するのは照合済みの航路だけにする。** 照合前の下書きは、公開データとは分けて扱う。
3. **OSM の取り込みと推定形状の計算は、人が手動で実行する。** 差分をレビューしてからコミットする。定期実行のワークフローは作らない（ADR 0003）。
4. **推定形状の計算だけは Python で書く**（`tools/` 配下、uv で管理）。それ以外は TypeScript で書く（ADR 0002）。
5. **TypeScript は 6.x に留める。** typescript-eslint がまだ TypeScript 7 に対応していないため。tsconfig は TypeScript 7 でもそのまま通る書き方にしておく。`baseUrl` や `moduleResolution: node10` は使わない。
6. **データ生成スクリプトは Node で `.ts` を直接実行する。**
   - 型を消すだけで動く構文（`erasableSyntaxOnly`）で書く。
   - 型だけの import は `import type` にする。
   - import のパスには `.ts` 拡張子を付ける。

## 作業の進め方

- **ブランチと PR**：作業はブランチで行い、PR にする。main へのマージは人が行う。
- **PR を出す前の確認**：`pnpm typecheck`、`pnpm lint`、`pnpm test` がすべて通ることを確かめる（CI と同じ内容）。画面に関わる変更では、`pnpm test:e2e` も通す。
- **コミットと PR の書き方**：日本語で書き、先頭に Conventional Commits の種別（`feat:` `fix:` `docs:` `chore:` など）を付ける。
- **コミットの作成者**：`namihagi <43953318+namihagi@users.noreply.github.com>` にする。これはリポジトリのローカル設定に入っている。新しくクローンした環境では、コミットする前に `git config user.email` を確認する。
- **言語**：ドキュメントは日本語で書く。コードの識別子は英語にし、データの値（港名、航路名など）は日本語にする。
- **ドキュメントの更新**：
  - 新しい用語が固まったら、`CONTEXT.md` を更新する。
  - 覆しにくい判断をしたら、`docs/adr/` に ADR を追加する。
- **仕様にない機能**：仕様にない機能や、仕様で「範囲外」とした機能を足したくなったら、実装する前に人に確認する。
- **照合の手順**：照合の手順は、最初の1〜2航路を照合しながら固め、固まったらプロジェクトのスキルにする。
